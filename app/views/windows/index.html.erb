<!DOCTYPE html>
<html>
<head>
	<title>Calendar</title>
</head>
	  <ul class="allwindows">
	    <% @windows.each do |window| %>
	    <li>
				<%= link_to image_tag(window.image), window_path(window.id)%>
	    </li>
	    <% end %>
	  </ul>
</html>
